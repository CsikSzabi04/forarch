# Non-web components
forarch/decay/
forarch/witness/
.venv/

# Build artifacts and local deps
node_modules/
dist/
.env*.local
.git/
.vscode/

# Ignore root-level vercel if we are building from website
# (But we keep it for now as a fallback)
